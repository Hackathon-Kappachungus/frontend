import { RandomMoveDirective } from './random-move.directive';

describe('RandomMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomMoveDirective();
    expect(directive).toBeTruthy();
  });
});
